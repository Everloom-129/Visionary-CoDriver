from setuptools import setup, find_packages

setup(
    name="vcd",
    version="0.1.0",
    description="Visionary CoDriver - An LLM-based driving risk perception system",
    author="Tony Wang",
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.8',
)
